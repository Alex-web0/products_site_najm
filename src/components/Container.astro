---
interface Props {
  makeCol?: boolean;
}

const { makeCol = false } = Astro.props;
---

<div class="w-full max-w-screen-xl mx-auto">
  <div
    class={(makeCol ? "flex flex-col" : " flex flex-row ") +
      "  justify-between p-2 md:py-3"}
  >
    <slot />
  </div>
</div>
